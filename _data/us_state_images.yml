# =============================================================================
# us_state_images.yml — optional flag/photo per US state for the stats chips
# =============================================================================
# List of {name, url} entries. Managed from /admin/ → Site Settings →
# "US state flag images". Same behavior as country_images.yml but keyed
# by full state name (e.g. "California", "New York").
# =============================================================================

entries: []
